kind: Gateway
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: external
  namespace: projectcontour
spec:
  gatewayClassName: external
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      allowedRoutes:
        namespaces:
          from: All
