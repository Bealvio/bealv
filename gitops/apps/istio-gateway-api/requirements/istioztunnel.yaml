apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istio-ztunnel
  namespace: flux-system
spec:
  interval: 1h0m0s
  targetNamespace: istio-system
  dependsOn:
    - name: istio-base
  chart:
    spec:
      chart: ztunnel
      version: '1.23.2'
      sourceRef:
        kind: HelmRepository
        name: istio
