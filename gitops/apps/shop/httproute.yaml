apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prestashop-http
  namespace: shop
spec:
  parentRefs:
    - name: https
      namespace: kgateway-system
      sectionName: http
  hostnames:
    - shop.bealv.io
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prestashop
  namespace: shop
spec:
  parentRefs:
    - name: https
      namespace: kgateway-system
      sectionName: https
  hostnames:
    - shop.bealv.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: prestashop
          port: 80
      filters:
        # "set" et non "add" : sans ecrasement, un X-Forwarded-Proto envoye par
        # le client se retrouverait en double et pourrait etre usurpe.
        - type: RequestHeaderModifier
          requestHeaderModifier:
            set:
              - name: X-Scheme
                value: https
              - name: X-Forwarded-Proto
                value: https
